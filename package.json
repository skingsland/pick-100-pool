{
  "name": "pick-100-pool",
  "version": "2.0.0",
  "description": "The web server for pick100pool.com, which has backend logic to download game info and calculate scores",
  "main": "web.js",
  "scripts": {
    "test": "jest",
    "test:e2e": "npx playwright test",
    "test:e2e:headed": "npx playwright test --headed",
    "test:setup-fixtures": "node e2e/fixtures/setup-testing-tournament.js"
  },
  "repository": "https://github.com/skingsland/pick-100-pool",
  "author": "skingsland",
  "dependencies": {
    "express": "^4.22.1",
    "firebase-admin": "^12.0.0",
    "got": "^11.8.6",
    "moment": "^2.29.1",
    "q": "^1.5.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "dotenv": "^17.3.1",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": "19.x"
  }
}
