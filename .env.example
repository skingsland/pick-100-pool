# Firebase Admin SDK service account key (JSON string) — used by theScore.js and fixture setup
GOOGLE_AUTH_JSON='...'

# Password for the e2e-tests@pick100pool.com Firebase Auth account
E2E_TEST_PASSWORD='...'
